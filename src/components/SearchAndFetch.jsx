import React from 'react'


// * input tag
// * has a button
// * inside of the input tag i can give a pokemon name
// * when i click the button i fetch the pokemon that i gave in the input
// * i display the name and image of the pokemon
export default function SearchAndFetch() {
  return (
    <div>SearchAndFetch</div>
  )
}
